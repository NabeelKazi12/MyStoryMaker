# Copia este fichero a `.env` y rellena los valores. `.env` esta en .gitignore: aqui no
# hay ni puede haber ninguna clave real.

# --- Modelo -------------------------------------------------------------------------
# No hace falta ninguna clave de API: el worker invoca a Haiku a traves de Claude Code
# (`claude -p --model haiku`) y reutiliza la sesion con la que Claude Code ya esta
# autenticado. Basta con haber iniciado sesion una vez ejecutando `claude`.
# Solo si `claude` no esta en el PATH del worker, da aqui su ruta completa:
# MYSTORYMAKER_CLAUDE=C:\Users\TU_USUARIO\AppData\Roaming\npm\claude.cmd

# --- Base de datos ------------------------------------------------------------------
MYSTORYMAKER_DB=./canon.db

# --- Observabilidad (fase D) --------------------------------------------------------
# Si no se rellenan, el sistema usa ClienteNulo y sigue funcionando: el observador nunca
# es punto unico de fallo.
LANGFUSE_PUBLIC_KEY=pk-lf-TU_CLAVE_AQUI
LANGFUSE_SECRET_KEY=sk-lf-TU_CLAVE_AQUI
LANGFUSE_HOST=https://cloud.langfuse.com
# El id del proyecto, el que aparece en la URL de Langfuse tras /project/. Solo sirve para
# que la pestaña «Gastos» enlace cada llamada a su traza; sin el, no hay enlaces (SPEC-012).
LANGFUSE_PROJECT_ID=TU_PROYECTO_AQUI

# --- API ----------------------------------------------------------------------------
# CORS por lista explicita, nunca comodin (RC-08).
MYSTORYMAKER_ORIGENES=http://localhost:5173
